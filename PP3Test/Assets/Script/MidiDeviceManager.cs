using UnityEngine;
using Minis;

public class MidiDeviceManager : MonoBehaviour
{
    void Start()
    {
        // List all connected MIDI devices
       // foreach (var device in MidiDevice.devices)
       // {
      //     Debug.Log("Found MIDI device: " + device.name);
       // }
    }
}